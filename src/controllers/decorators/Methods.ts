// Create enum that defines all the different HTTP methods
// we want to use in one place
// Enum naming convention to use capital for enum members

export enum Methods {
  Get = 'get',
  Post = 'post',
  Patch = 'patch',
  Del = 'delete',
  Put = 'put',
}
